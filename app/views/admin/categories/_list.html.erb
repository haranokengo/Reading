<table class="table">
  <thead>
    <tr>
      <th>カテゴリー名</th>
      <th></th>
      <th></th>
    </tr>
      </thead>
    <tbody>
    <% categories.each do |category| %>
      <tr>
        <td><%= category.name %></td>
        <td><%= link_to '編集', edit_admin_category_path(category.id) %></td>
        <td><%= link_to '削除', admin_category_path(category.id), method: :delete, "data-confirm" => "本当に削除しますか？" %></td>
      </tr>
    <% end %>
    </tbody>
</table>